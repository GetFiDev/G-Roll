name: Build Automation 🎮

on: push

jobs:
  testRunner:
    if: startsWith(github.event.head_commit.message, 'build:')
    name: Test in ${{ matrix.targetPlatform }} ✨
    runs-on: self-hosted
    strategy:
      fail-fast: false
      matrix:
        targetPlatform:
          - iOS

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Create LFS file list
        run: git lfs ls-files -l | cut -d' ' -f1 | sort > .lfs-assets-id

      - name: Restore LFS cache
        uses: actions/cache@v3
        id: lfs-cache
        with:
          path: .git/lfs
          key: ${{ runner.os }}-lfs-${{ hashFiles('.lfs-assets-id') }}

      - name: Git LFS Pull
        run: |
          git lfs pull
          git add .
          git reset --hard

      - name: Build with Unity
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: ${{ matrix.targetPlatform }}
          
      - name: Save build outside workspace
        run: |
          ARTIFACTS_DIR=$(dirname "${GITHUB_WORKSPACE}")/artifacts/${{ matrix.targetPlatform }}
          mkdir -p "$ARTIFACTS_DIR"
          cp -r build/${{ matrix.targetPlatform }}/* "$ARTIFACTS_DIR"/
          echo "Build saved to $ARTIFACTS_DIR"

      - name: Restore Library cache
        uses: actions/cache@v3
        with:
          path: Library
          key: Library-${{ matrix.targetPlatform }}
          restore-keys: Library-
